<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %> - Admin
    </title>
    <script src="https://cdn.tailwindcss.com"></script>
    <%- include('head_config') %>
</head>

<body class="bg-gray-50 flex h-screen">
    <!-- Sidebar -->
    <%- include('sidebar') %>

        <main class="flex-1 overflow-y-auto p-8 bg-gray-50/50">
            <div class="flex justify-between items-center mb-8 max-w-7xl mx-auto">
                <h1 class="text-3xl font-bold text-gray-800">
                    <%= title %>
                </h1>
                <a href="<%= createLink %>"
                    class="bg-salihara-red text-white px-6 py-2.5 rounded-full hover:bg-red-600 transition font-bold text-sm shadow-lg shadow-red-200 flex items-center gap-2">
                    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4" />
                    </svg>
                    Tambah Baru
                </a>
            </div>

            <div class="bg-white rounded-2xl shadow-sm border border-gray-100 overflow-hidden max-w-7xl mx-auto">
                <table class="min-w-full divide-y divide-gray-100">
                    <thead class="bg-gray-50/50">
                        <tr>
                            <th
                                class="px-8 py-4 text-left text-[10px] font-bold text-gray-400 uppercase tracking-widest">
                                Judul / Judul Karya
                            </th>
                            <th
                                class="px-8 py-4 text-left text-[10px] font-bold text-gray-400 uppercase tracking-widest text-center">
                                Tanggal Terbit
                            </th>
                            <th
                                class="px-8 py-4 text-right text-[10px] font-bold text-gray-400 uppercase tracking-widest">
                                Kontrol</th>
                        </tr>
                    </thead>
                    <tbody class="divide-y divide-gray-50 bg-white">
                        <% items.forEach(item=> { %>
                            <tr class="hover:bg-gray-50/50 transition-colors group">
                                <td class="px-8 py-5 whitespace-nowrap">
                                    <div
                                        class="text-sm font-semibold text-gray-800 group-hover:text-salihara-red transition-colors">
                                        <%= item.title %>
                                    </div>
                                    <div class="text-[10px] text-gray-400 uppercase mt-0.5">
                                        <%= item.category || 'Content' %>
                                    </div>
                                </td>
                                <td class="px-8 py-5 whitespace-nowrap text-center">
                                    <div
                                        class="text-[11px] font-medium text-gray-500 bg-gray-100 px-3 py-1 rounded-full display-inline-block">
                                        <%= new Date(item.created_at).toLocaleDateString('id-ID', { day: '2-digit' ,
                                            month: 'short' , year: 'numeric' }) %>
                                    </div>
                                </td>
                                <td class="px-8 py-5 whitespace-nowrap text-right text-sm">
                                    <div
                                        class="flex justify-end gap-3 opacity-0 group-hover:opacity-100 transition-opacity">
                                        <a href="<%= basePath %>/edit/<%= item.id %>"
                                            class="text-gray-400 hover:text-indigo-600 p-2 hover:bg-indigo-50 rounded-lg transition-all"
                                            title="Edit">
                                            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                                    d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z" />
                                            </svg>
                                        </a>
                                        <a href="<%= basePath %>/delete/<%= item.id %>"
                                            class="text-gray-400 hover:text-red-600 p-2 hover:bg-red-50 rounded-lg transition-all"
                                            onclick="return confirm('Hapus karya ini?')" title="Hapus">
                                            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                                    d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16" />
                                            </svg>
                                        </a>
                                    </div>
                                </td>
                            </tr>
                            <% }); %>
                                <% if (items.length===0) { %>
                                    <tr>
                                        <td colspan="3" class="px-8 py-12 text-center">
                                            <div class="text-gray-300 mb-2 font-serif text-3xl">Kosong</div>
                                            <div class="text-gray-400 text-xs uppercase tracking-widest">Belum ada karya
                                                yang diunggah</div>
                                        </td>
                                    </tr>
                                    <% } %>
                    </tbody>
                </table>
            </div>
        </main>
</body>

</html>