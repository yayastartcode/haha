<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Site Settings - Admin</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <%- include('head_config') %>
</head>

<body class="bg-gray-100 font-sans">
    <div class="flex h-screen">
        <%- include('sidebar') %>

            <main class="flex-1 overflow-y-auto p-8">
                <div class="max-w-4xl">
                    <h2 class="text-2xl font-bold text-gray-800 mb-6">Site Settings</h2>

                    <% if (typeof success !=='undefined' && success) { %>
                        <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded mb-6">
                            Settings updated successfully!
                        </div>
                        <% } %>

                            <form action="/admin/settings" method="POST"
                                class="bg-white shadow-md rounded px-8 pt-6 pb-8 mb-4">
                                <div class="mb-6 border-b pb-4">
                                    <h3 class="text-lg font-semibold text-gray-700 mb-4">Footer Information</h3>

                                    <div class="mb-4">
                                        <label class="block text-gray-700 text-sm font-bold mb-2">Address</label>
                                        <textarea name="footer_address"
                                            class="shadow appearance-none border rounded w-full py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:shadow-outline"
                                            rows="4"><%= siteSettings.footer_address %></textarea>
                                        <p class="text-xs text-gray-500 mt-1 italic">Use new lines to separate address
                                            parts</p>
                                    </div>

                                    <div class="mb-4">
                                        <label class="block text-gray-700 text-sm font-bold mb-2">Contact Button
                                            Label</label>
                                        <input type="text" name="footer_contact_label"
                                            value="<%= siteSettings.footer_contact_label %>"
                                            class="shadow appearance-none border rounded w-full py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:shadow-outline">
                                    </div>
                                </div>

                                <div class="mb-6">
                                    <h3 class="text-lg font-semibold text-gray-700 mb-4">Social Media Links</h3>

                                    <div class="mb-4">
                                        <label class="block text-gray-700 text-sm font-bold mb-2">Instagram URL</label>
                                        <input type="text" name="instagram_url"
                                            value="<%= siteSettings.instagram_url %>"
                                            class="shadow appearance-none border rounded w-full py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:shadow-outline">
                                    </div>

                                    <div class="mb-4">
                                        <label class="block text-gray-700 text-sm font-bold mb-2">Twitter URL</label>
                                        <input type="text" name="twitter_url" value="<%= siteSettings.twitter_url %>"
                                            class="shadow appearance-none border rounded w-full py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:shadow-outline">
                                    </div>

                                    <div class="mb-4">
                                        <label class="block text-gray-700 text-sm font-bold mb-2">Facebook URL</label>
                                        <input type="text" name="facebook_url" value="<%= siteSettings.facebook_url %>"
                                            class="shadow appearance-none border rounded w-full py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:shadow-outline">
                                    </div>
                                </div>

                                <div class="flex items-center justify-end">
                                    <button type="submit"
                                        class="bg-[#FA7268] hover:bg-red-600 text-white font-bold py-2 px-6 rounded focus:outline-none focus:shadow-outline transition">
                                        Save Settings
                                    </button>
                                </div>
                            </form>
                </div>
            </main>
    </div>
</body>

</html>